v4.15: 统一认证直登、洗衣下单与支付修复、课表数据源策略、启动崩溃修复

新增
- 统一身份认证登录:OAuth2 协议直登(SM2 加密),登录页双模式液态玻璃切换;
  验证码/MFA 场景保留网页登录降级;支持记住统一认证凭据、重启自动登录
- 课表:手动导入教务导出的 XLS 文件 + "使用导入的课表"开关(开启后不联网刷新,
  仅切换学期/手动刷新时联网);离线/未登录自动回退缓存课表
- 洗衣:下单可选温度与洗衣液/除菌液加购(按设备接口返回)、实时预估价、
  "支付成功后自动启动洗衣机"开关;已保存洗衣机选中样式与空闲/忙碌状态
- 课表页头部新增手动刷新;成绩/选课等页未登录提示优化

修复
- 启动崩溃:部分状态声明在 init 之后导致初始化期 NPE(本周第 0 周、启动闪退的根因)
- 洗衣:订单金额显示(元/分换算)、支付宝拉起改用官方 SDK(此前跳到扫码页)、
  已保存设备直接下单失败、导入缓存落盘时机
- 课表:周次计算时区偏移(0-8 点显示为上周)、第一周周一设置不生效、
  登录失败场景下重启不显示缓存
- 选课:登录过期时轮次被当作"无轮次"(302 误判),现明确提示重新登录

性能
- 课表翻页预计算 + 模型 @Immutable、主题切换去逐色动画、
  课表/选课缓存直出、三请求并发、扫码分析限 720p

其他
- 固定发布签名(应用内更新可覆盖安装)、更新包 sha256 完整性校验、
  CI 自动写入更新元数据、R8 压缩(体积约 -44%)
- 移除底栏滑动切页、删除"我的"页重复饮水入口、删除 11-12 节

升级说明:v4.14 及更早版本为随机签名,升级到本版本需先卸载旧版(会清除本地数据);
自本版本起应用内更新可正常覆盖安装。
